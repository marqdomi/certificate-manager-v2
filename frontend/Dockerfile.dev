# frontend/Dockerfile.dev

# 1. Usamos una imagen oficial de Node.js
FROM node:20

# 2. Establecemos el directorio de trabajo dentro del contenedor
WORKDIR /app

# 3. Copiamos los archivos que definen las dependencias de JavaScript
COPY package.json package-lock.json* ./
# El '*' al final de package-lock.json* es un truco para que no falle si el archivo no existe la primera vez.

# 4. Instalamos las dependencias de JavaScript
RUN npm install

# 5. Copiamos el resto del código fuente del frontend
COPY . .

# 6. Exponemos el puerto que usa Vite y lo iniciamos
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host"]